version: '3.8'

services:
  sportif-backend:
    build: .
    container_name: sportif_backend
    ports:
      - "8002:8002"
    # Enables host.docker.internal to resolve the linux host IP
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      # Database Config (Passed from your .env file)
      - DB_HOST=${DB_HOST}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - DB_PORT=${DB_PORT}
      - DB_SSLMODE=${DB_SSLMODE}
      
      # App Config
      - PORT=8002
      - JWT_SECRET=${JWT_SECRET}
      
      # Cloudflare R2 Config
      - R2_ENDPOINT=${R2_ENDPOINT}
      - R2_ACCESS_KEY_ID=${R2_ACCESS_KEY_ID}
      - R2_SECRET_ACCESS_KEY=${R2_SECRET_ACCESS_KEY}
      - R2_BUCKET_NAME=${R2_BUCKET_NAME}
      - R2_PUBLIC_URL=${R2_PUBLIC_URL}
      
      # Redis Config (Passed from your .env file)
      - REDIS_HOST=${REDIS_HOST}
      - REDIS_PORT=${REDIS_PORT}
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - REDIS_DB=${REDIS_DB}
    networks:
      - e-c-deployerscript_e-commerce-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  e-c-deployerscript_e-commerce-network:
    external: true